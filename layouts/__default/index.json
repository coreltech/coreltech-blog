[
  {{ range first 5 site.RegularPages }}
    {
      "title": "{{ .Title }}",
      "link": "{{ .Permalink }}",
      "date": "{{ .Date.Format "2006-01-02" }}"
    }{{ if not (lastindex (first 5 site.RegularPages) .) }},{{ end }}
  {{ end }}
]
